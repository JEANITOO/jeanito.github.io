name: aplicativo_responsivo
description: Um aplicativo Flutter responsivo para diferentes tamanhos de tela.

# Versão do seu projeto
version: 1.0.0+1

# Ambiente do Flutter
environment:
  sdk: ">=3.6.0 <4.0.0"  # Definir o intervalo de versões para o SDK do Dart

dependencies:
  flutter:
    sdk: flutter

  # Caso precise de mais dependências, adicione aqui.

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
